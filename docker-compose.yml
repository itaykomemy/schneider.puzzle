version: '3'

services:
  web:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    environment: 
      - DEBUG=True
    volumes:
      - ./db:/home/docker/db
    ports:
      - "8000:8000/tcp"
